<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.SkiItemMapper">
    <select id="selectSkiItem" parameterType="map"  resultType="dto.SkiItemDTO">
        select * from ski_item where category_id = #{category_id} and ski_id = #{ski}
    </select>

    <select id="getList"  parameterType="map" resultType="dto.SkiItemDTO" >
        select * from ski_item where category_id = #{category_id} and ski_id = #{ski}
    </select>

    <select id = "getSkiItemByItemId" parameterType="int" resultType="dto.SkiItemDTO" >
        select item_id, category_id, rentalshop_id, item_name, item_img, item_type, price_per_hour
        from
    </select>

</mapper>
